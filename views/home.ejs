<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />
    <title>ProbList</title>
</head>
<body>
    <h1>Categories Of Problem</h1>
    <form action="/" method="post">
        <!-- name is what goes to the server -->
        <label for="catname" >New Catagory Name</label>
        <input type="text" id="catname" name = "catname">
        <input type="submit" value="Submit">
    </form>

    <a href="/addprob">Add Problem</a>

    <% for(i=0 ; i<  cat.length ;i++){ %>
        <div>
            <h1>
                <%= cat[i].category %>
            </h1>
            <% for(j=0 ;j < cat[i].prob.length ; j++){%>
                <div>
                <a href = <%= cat[i].prob[j].link %>> <%= cat[i].prob[j].link %></a>
                <div><%= cat[i].prob[j].count %></div>
                <input type="button" value="+" class = "incre_btn" id = "btn<%= cat[i].prob[j].id %>">
                </div>
            <% }%>
        </div>
    <% } %>

</body>
<script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.6.0.js"></script>
<script src="../scripts/home_scripts.js"></script>
</html>