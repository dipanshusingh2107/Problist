<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />
    <title>ProbList</title>
    <!-- <link rel="stylesheet" href="/styles/css/home.css"> -->
    <!-- bootstrap cdn -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body style ="background-color: #E4F9F5">
   

    <a href="/addprob">Add Problem</a>

    <% for(i=0 ; i<  cat.length ;i++){ %>
        <div>
            <h1>
                <%= cat[i].category %>
            </h1>
            <% for(j=0 ;j < cat[i].prob.length ; j++){%>
                <div class="container">

                    <div class="row mt-1">

                        <div class="col-8">
                            <!-- rel="noopener noreferrer" is to prevent phishing from tabnabbing -->
                        <a href = <%= cat[i].prob[j].link %> target = "_blank" rel="noopener noreferrer"> <%= cat[i].prob[j].link %></a>
                        </div>

                        <div class="col-1">
                        <div id="cnt<%=cat[i].prob[j].id%>"><%= cat[i].prob[j].count %></div>
                        </div>

                        <div class="col-1">
                        <input type="button" value="+" class = "incre_btn btn btn-outline-success" id = "btn<%= cat[i].prob[j].id %>">
                        </div>
                    
                    </div>
                </div>
            <% }%>
        </div>
    <% } %>

</body>
<script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.6.0.js"></script>
<script src="../scripts/home_scripts.js"></script>
</html>